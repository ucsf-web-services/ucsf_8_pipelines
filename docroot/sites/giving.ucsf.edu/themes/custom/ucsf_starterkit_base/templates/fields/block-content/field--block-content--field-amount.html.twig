{% for item in items %}
    <div class="ways-to-give-option" role="button">
        <input type="radio" name="give-value" id="give_value_{{ item.attributes.content }}" value="{{ item.attributes.content }}" />
        <label for="give_value_{{ item.attributes.content }}">{{ item.content['#markup'] }}</label>
    </div>
{% endfor %}